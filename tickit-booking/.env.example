# Copy to .env (git-ignored) before starting the stack:  copy .env.example .env
# Factor III: configuration & secrets live here, never in source code.
POSTGRES_DB=tickit
POSTGRES_USER=tickit
POSTGRES_PASSWORD=change_me_in_real_envs
